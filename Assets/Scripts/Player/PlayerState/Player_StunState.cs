using UnityEngine;

public class Player_StunState : PlayerState
{
    public Player_StunState(Player player, StateMachine stateMachine, string animParam) : base(player, stateMachine, animParam)
    {
    }
}
